import { Routes } from '@angular/router';
import { LoginPage } from '../formulario/formulario/formulario';

export const routes: Routes = [
    {
        path:'',
        component:LoginPage
    }
];
